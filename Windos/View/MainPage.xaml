<Page
    x:Class="Windos.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Windos.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:media="using:Windows.UI.Xaml.Media"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d">
    <StackPanel Background="{ThemeResource AcrylicBackgroundBrush40}">
        <ListView ItemsSource="{x:Bind navButtons}"
            IsItemClickEnabled="True"
            SelectionMode="None"
                  ItemClick="ListView_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:NavButton">
                    <controls:DropShadowPanel BlurRadius="8" ShadowOpacity="0.7" 
                                              HorizontalAlignment="Center"
                                    OffsetX="2" OffsetY="2" Color="{x:Bind Bg}" Margin="20">
                        <Button Content="{x:Bind Title}" Background="{x:Bind Bg}"/>
                    </controls:DropShadowPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
